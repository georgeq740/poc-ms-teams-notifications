{
    "name": "poc-ms-teams-notifications",
    "version": "1.0.0",
    "description": "GitHub Action for sending notifications to Microsoft Teams",
    "main": "dist/index.js",
    "scripts": {
        "build": "ncc build src/index.js -o dist",
        "lint": "eslint src --fix"
    },
    "dependencies": {
        "@actions/core": "^1.10.0",
        "axios": "^1.3.4"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.34.0",
        "eslint": "^8.43.0"
    }
}
